import style from "./Application.module.css";

export const Stats = () => {
  return <section className={style.stats}> Stats </section>;
};

// if everything is related it is best to put it under one section
